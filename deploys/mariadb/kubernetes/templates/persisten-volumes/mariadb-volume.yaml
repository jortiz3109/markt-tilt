apiVersion: v1
kind: PersistentVolume
metadata:
  name: mariadb-pv-volume
spec:
  storageClassName: standard
  capacity:
    storage: 300M
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/markit/mariadb-pv-volume/